import './CSS/MoreButton.css'

const MoreButton = ({rightButton}) => {
    
    return(
        <a className="rightButton">
            {rightButton}
        </a>
    )
    
}
export default MoreButton;