import './CSS/ShareButton.css'

const ShareButton = ({leftButton}) => {
    
    return(
        <a className="leftButton">
            {leftButton}
        </a>
    )
    
}
export default ShareButton;